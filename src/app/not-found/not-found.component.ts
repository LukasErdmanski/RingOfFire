import { Component } from '@angular/core';

/**
 * Component representing a "Not Found" page.
 *
 * This component is typically displayed when a user navigates to a route or URL that doesn't exist in the application.
 * It provides visual feedback to the user indicating that the requested page could not be found.
 */
@Component({
    selector: 'app-not-found',
    templateUrl: './not-found.component.html',
    styleUrls: ['./not-found.component.scss'],
})
export class NotFoundComponent {}