import { Component } from '@angular/core';

/**
 * A component to display information and prompt the user to rotate their device.
 * This  application that have a preferred orientation.
 */
@Component({
    selector: 'app-rotate-device-info',
    templateUrl: './rotate-device-info.component.html',
    styleUrls: ['./rotate-device-info.component.scss'],
})
export class RotateDeviceInfoComponent {}